// TODO: Phase 4 — AES-256-GCM encryption with OS keychain
// For now, API keys are stored in plaintext in SQLite.

pub fn encrypt(_plaintext: &str) -> String {
    _plaintext.to_string()
}

pub fn decrypt(_ciphertext: &str) -> String {
    _ciphertext.to_string()
}
